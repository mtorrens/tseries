################################################################################
predictor.team <- function(y) {
################################################################################
  f <- jitter(y[nrow(y), 1])
  
  return(f)
}
# END OF SCRIPT
