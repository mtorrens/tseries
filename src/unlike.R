################################################################################
predictor.unlike <- function(y) {
################################################################################
  pred <- jitter(y[nrow(y), 1])
  
  return(pred)
}
# END OF SCRIPT
